{
  "name": "zk-voting-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src/backend",
    "src/frontend",
    "src/tx"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "dev": "npm run dev --workspaces",
    "install:tx": "cd src/tx && npm install",
    "build:tx": "cd src/tx && npm run build"
  },
  "dependencies": {
    "@nestjs/config": "^4.0.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "dotenv": "^16.0.3"
  }
}
